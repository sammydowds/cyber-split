import { SplitDeep } from "@/types";
import { Card } from "../../../ui/card";

interface SettingsProps {
  split: SplitDeep;
}
export const Settings = ({ split }: SettingsProps) => {
  return <Card>TODO</Card>;
};
