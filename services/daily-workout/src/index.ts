import { prisma } from "@repo/database";
import assert from "assert";

const textUsers = async () => {};
// get user info
// get user active split
// get user workout today
// if user phone and notifications enabled and workout today
// send text
